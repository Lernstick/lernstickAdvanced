- dkms changes in wheezy are also needed in jessie (build for all kernels!)

- boot options for system and persistence media with preference and fallback instead of hard selection

- set keyboard at every boot
	- disable 0150-keyboard-configuration and 1060-kde-services in live-config
	- Cinnamon & MATE: autostart script successfully updates layout list but is too late
	  to update the selection (previously selected layout is kept until next login)
	- Enlightenment:
		- change config via eet (./e/config/standard/e.cfg E_Config_XKB_Layout)

- persistence media boot option does not get written back to bootloader
	- is "usb" a supported option?
	- manpage says only removable|removable-usb, see
	  http://live.debian.net/manpages/stable/en/html/live-boot.7.html

- live-swapfile needs interaction with the user at system startup
	- as systemd service?
		- start before display managers
		- dialog needs a getty
	- in live-config?
		- plymouth user interaction
			- simply sucks
			- availability is not guaranteed
		- gettys are not there yet

- new world 2 doesn't work with chromium?

- remote support setup so that Gaudenz can also provide support

- live-guest
	- update for jessie
	- add fix script for owncloud folders (it always uses full paths but would work with relative paths)

- live build auto config does not work for me (contrib and non-free is missing in chroot)

- lernstickWelcome:
	- add findbugs to netbeans package ("Java development package"?)
	- makehuman
	- detect and use correct architecture (i386 or amd64) for:
		- google earth
		- skype

- efi blacklist
	- also blacklist kernel driver (test on MacBook)

- test new programs:
	- flightgear
	- fgrun

- Test if hp-wmi rfkill workaround is still needed. See https://bugzilla.kernel.org/show_bug.cgi?id=76471

- mac volume label still doesn't seem to work (tested on Mac@home)

- disable screen lock in GNOME

- no tty after X on MacDesktop@work and Mac@home
	- see https://bbs.archlinux.org/viewtopic.php?id=143569
	- try boot options "video=vesa:off vga=normal"

- dlcopy:
	- when data partition of source is read-only, data partition mode shows "read and write"
	- create a new uuid when copying or creating a new image

- detect connection loss to system storage media and show notification to user

- put third party debs into our repository:
	- alice2
	- cmaptools (license!?)
	- kara
	- lxmed
- package software
	- tuxmathscrabble (http://www.asymptopia.org/software/tuxmathscrabble/)
	- freedoko http://free-doko.sourceforge.net/de/FreeDoko.html
	- xlogo (http://xlogo.tuxfamily.org/)
	- xlogo4schools (http://sourceforge.net/projects/xlogo4schools/)

- Audacity: high-res icon

- exam environment: automatic/interactive file sytem repair for damaged (read-only mounted) exchange paritition?

- MacBook@work:
	- kdm timeout seems to be too small (starting with DVD)

- remote support on HTTPS (easier with Swisscom firewalls)
	- add https://meet.jit.si/lernstick to chromium bookmarks
	- preinstall jitsi desktop streamer plugin in chromium

- JFractionLab: creating worksheets doesn't work

- user template:
	- iceweasel: default bookmarks
	- xmoto: minimal settings

- live-startup-scripts for:
	- megaglest
	- hedgewars

- test if we still need these packages from wheezy:
	- lernstick-kmix
	- lernstick-plymouth

- blender: activate add-on “Add Mesh Extra Objets”

- tts package (gespeaker + mbrola voices for supported languages)

- backports:
	- todo:
		- cinnamon
		- efibootmgr
		- exfat-utils
		- firmware-nonfree
		- gnome
		- i965-va-driver
		- linux (including aufs!?)
			- patching in aufs (see aufs README):
				- cd linux-sources
				- patch -p1 </home/blah/aufs4-standalone/aufs4-base.patch
				- patch -p1 </home/blah/aufs4-standalone/aufs4-kbuild.patch
				- patch -p1 </home/blah/aufs4-standalone/aufs4-mmap.patch
				- patch -p1 </home/blah/aufs4-standalone/aufs4-standalone.patch
				- copy ./{Documentation,fs,include/uapi/linux/aufs_type.h} files to your  kernel source tree
				- adopt debian/config/config (see previous kernels) to build module
				- change debian/config/defines e.g.:
					- abiname: 2-lernstick
			- build all packages:
				- fakeroot make -f debian/rules.gen setup_i386_none_686-pae
				- fakeroot make -f debian/rules.gen binary-arch_i386_none_686-pae DEBIAN_KERNEL_JOBS=8
				- fakeroot make -f debian/rules.gen setup_i386_none_586
				- fakeroot make -f debian/rules.gen binary-arch_i386_none_586 DEBIAN_KERNEL_JOBS=8
			- build common headers:
				- fakeroot make -f debian/rules.gen binary-arch_i386_none_real DEBIAN_KERNEL_JOBS=8
			- build linux-compiler package:
				- fakeroot make -f debian/rules.gen binary-arch_i386_extra DEBIAN_KERNEL_JOBS=8
			- build libc-dev package:
				- fakeroot make -f debian/rules.gen binary-libc-dev_i386 DEBIAN_KERNEL_JOBS=8
			- backport linux-tools
			- cleanup: make -f debian/rules clean
		- mate
		- phoronix-test-suite (orphaned!?)
		- xorg-server
	- backports pinned to official backports but newer version available:
		- darktable
		- youtube-dl
	- backports with bugs:
		- blender (international fonts are not available)
		- fritzing (needs fritzing-parts which is not yet available)
		- miro (freezes in youtube section)
		- nvidia-graphics-drivers (failure during dkms install)
		- openttd (needs newer gfx package)
		- stopmotion (program crashes)
		- supertuxkart (system crash, tested on Mac@Michelle and Latitude D630) 
		- virtualbox (breaks KDE in virtualbox)
	- backporting failed:
		- ardour3
		- calibre
		- netbeans
		- owncloud
		- pentobi (dependencies to KDE 5)
		- pspp 0.8.5 (initdb: cannot be run as root)
		- xfce4
			- error while backporting libxfce4util-4.12.1:
				dh_install: libxfce4util-dev missing files (usr/share/gtk-doc/html/libxfce4util/*), aborting
